export const getAuthToken = (): string | null => {
    return null;
};

export const clearAuthToken = (): boolean => {
    return true;
};

export const saveNewToken = (): boolean => {
    return true;
};